---
phase: 02-provider-plugin-passthrough-gateway
plan: 07
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-04", "02-05", "02-06"]
files_modified:
  - src/hermes_auto/supervisor.py
  - src/hermes_auto/cli.py
  - src/hermes_auto/commands.py
  - src/hermes_auto/plugin.py
  - pyproject.toml
  - tests/integration/test_supervisor.py
  - tests/unit/test_cli.py
files_forbidden:
  - design.md
  - .planning/
  - src/hermes_auto/gateway/
  - src/hermes_auto/state/
  - src/hermes_auto/config.py
  - src/hermes_auto/telemetry/
  - src/hermes_auto/data/
  - src/hermes_auto/provider.py
  - src/hermes_auto/hermes_shim/
  - tests/fixtures/
  - tests/contract/
  - tests/integration/mock_upstream.py
  - .github/
  - docs/
sequential_files: []
expected_artifacts:
  - path: "src/hermes_auto/supervisor.py"
    provides: "Cross-platform supervision keyed on instance_id rather than PID"
    required: true
  - path: "src/hermes_auto/cli.py"
    provides: "hermes auto setup|start|stop|restart|status|doctor"
    required: true
  - path: "src/hermes_auto/plugin.py"
    provides: "Control plugin registering CLI subcommands, slash commands, and lifecycle hooks"
    required: true
autonomous: false
agents: [engineering-infrastructure-devops, engineering-senior-developer]
requirements: [R2, R4]
user_setup: []
verification_commands:
  - "for f in supervisor.py cli.py commands.py plugin.py; do test -f \"src/hermes_auto/$f\" || exit 1; done"
  - "./.venv/Scripts/python.exe -m pytest tests/integration/test_supervisor.py tests/unit/test_cli.py -q"
  - "./.venv/Scripts/python.exe -c \"from hermes_auto.cli import build_parser; sub={a.dest for a in build_parser()._subparsers._group_actions}; import argparse; p=build_parser(); names=set(p._subparsers._group_actions[0].choices); need={'setup','start','stop','restart','status','doctor'}; assert need<=names, sorted(need-names)\""
  - "./.venv/Scripts/python.exe -c \"import inspect, hermes_auto.supervisor as s; src=inspect.getsource(s); assert 'instance_id' in src, 'supervision must key on instance_id, not PID'; assert 'shutdown' in src, 'admin shutdown must be the primary stop path'\""
  - "./.venv/Scripts/python.exe -c \"import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert 'hermes-auto' in str(d['project'].get('scripts',{})), d['project'].get('scripts')\""
  - "./.venv/Scripts/python.exe -m pytest tests/ -q"
  - "git status --porcelain -- src/hermes_auto/gateway src/hermes_auto/state src/hermes_auto/config.py src/hermes_auto/telemetry src/hermes_auto/provider.py src/hermes_auto/hermes_shim tests/contract | grep -q . && exit 1 || exit 0"

must_haves:
  truths:
    - "status and doctor never trust the PID. They GET /healthz on the recorded port and compare the returned instance_id to the runtime file — the only PID-reuse-proof check available without psutil."
    - "stop tries POST /admin/v1/shutdown first on every platform, because Windows has no SIGTERM."
    - "restart waits for instance_id to change, which is what makes 'restart safe mid-session, no stale PID' a testable claim."
    - "The sidecar is spawned detached with stdout and stderr to a rotating file, never a pipe — a full pipe deadlocks an unattended sidecar."
    - "hermes auto setup installs the provider shim, because model providers are discovered by directory scan and pip install alone never registers this provider."
    - "doctor reads back the effective token permissions rather than assuming the write succeeded, since os.chmod is a no-op on Windows ACLs."
  artifacts:
    - path: "src/hermes_auto/supervisor.py"
      provides: "Spawn, stop, restart, status"
      min_lines: 120
      contains: "instance_id"
    - path: "src/hermes_auto/plugin.py"
      provides: "Hermes control plugin"
      min_lines: 60
      contains: "register"
  key_links:
    - from: "src/hermes_auto/supervisor.py"
      to: "src/hermes_auto/state/runtime.py"
      via: "reads the runtime file and compares instance_id"
      pattern: "read_runtime\\|instance_id"
---

<objective>
Supervise the sidecar across Windows, macOS, and Linux, and expose it through `hermes auto` subcommands, slash commands, and lifecycle hooks.

Purpose: R2 and R4's supervision half. The hard part is not starting a process — it is knowing whether the process you recorded is still the process that is running, on a platform where PID reuse is fast and `SIGTERM` does not exist.
Output: `supervisor.py`, `cli.py`, `commands.py`, `plugin.py`, the console-script entry, and their tests.
</objective>

<context>
@.planning/phases/02-provider-plugin-passthrough-gateway/02-CONTEXT.md — § Supervision Contract is authoritative and was frozen in Wave 1
@.planning/phases/02-provider-plugin-passthrough-gateway/02-01-SUMMARY.md — the runtime-file shape and token permission mechanism
@.planning/phases/02-provider-plugin-passthrough-gateway/02-04-SUMMARY.md — the gateway entry point and `/healthz` contract
@.planning/phases/02-provider-plugin-passthrough-gateway/02-05-SUMMARY.md — the shim installer `setup` calls, and the shim/provider duplication `doctor` should check for drift
@.planning/phases/02-provider-plugin-passthrough-gateway/02-06-SUMMARY.md — the admin shutdown path and its token
@design.md — §4.2 (the CLI and slash-command surface), §11.3 (sidecar outage layers). Grep; READ-ONLY.
</context>

<execution_contract>
Harness: read-before-write -> evidence-before-action -> minimal diff -> verify-before-report

Role: `engineering-infrastructure-devops` owns supervision and cross-platform process handling. `engineering-senior-developer` owns the CLI, the control plugin, and the Hermes-side registration.
Task: Deliver R2 and the supervision half of R4.
Scope:
- Read targets: `02-CONTEXT.md` § Supervision Contract; all five prior summaries; `design.md` §4.2, §11.3.
- Write targets: exactly `files_modified`. `pyproject.toml` is in scope **only** for the `[project.scripts]` console-script entry and the `hermes_agent.plugins` entry point — no dependency changes.
- Forbidden: everything listed.
Allowed tools/actions: read listed files; create `files_modified`; run `./.venv/Scripts/python.exe`, pytest, and start/stop a real sidecar locally.
Forbidden actions:
- Do not add a dependency. In particular do not add `psutil` — the `instance_id` check replaces it and works identically on all three platforms.
- Do not implement a systemd unit, launchd plist, or Windows Service. Start-on-demand covers CLI, TUI, and cron; persistent service mode is `design.md` §11.3 layer 2 and belongs to Phase 11.
- Do not implement `/auto mode`, `/auto explain`, `/auto candidates`, `/auto pin`, `/auto reroute`, or `/auto feedback` — all describe routing that does not exist. Register `/auto status` only.
- Do not modify any `gateway/` module.
- Do not self-defer planned work.
Implementation sequence: supervisor (Task 1) → CLI + console script (Task 2) → control plugin + tests (Task 3).
Required interfaces/content structure:
- `supervisor.py` exports `start()`, `stop(timeout=10.0)`, `restart()`, `status() -> Status`, and `SupervisorError`. `Status` is a frozen dataclass with `running: bool`, `instance_id: str | None`, `port: int | None`, `pid: int | None`, `detail: str`.
- `cli.py` exports `build_parser()` and `main(argv=None) -> int`.
- `commands.py` holds the command implementations so `cli.py` stays thin and the plugin can call them without re-parsing.
- `plugin.py` exports `register(ctx)`.
Edge/error cases:
- Runtime file absent → `status()` reports not running, no error.
- Runtime file present, `/healthz` refuses connection → stale; remove the file and report it was cleaned.
- Runtime file present, `/healthz` answers with a **different** `instance_id` → another process owns the port; report it and do **not** kill anything.
- `start()` when already running → no-op returning the existing `Status`, not an error.
- `stop()` when not running → returns cleanly.
- `restart()` must observe `instance_id` change; if the new instance does not appear within the timeout, `SupervisorError` naming the timeout.
- Windows: spawn with `CREATE_NEW_PROCESS_GROUP | DETACHED_PROCESS`, falling back to adding `CREATE_BREAKAWAY_FROM_JOB` when the parent sits in a job object (common on CI runners), where a detached child is otherwise killed with the parent.
Verification criteria: all seven `verification_commands` exit 0.
Final result format: Status | Files | Verification Commands Run/Passed/Failed | Verification | Decisions | Issues | Errors
</execution_contract>

<stop_gates>
Emit `BLOCKED` when: `/healthz` does not echo `instance_id` (plan 02-04 owes it); `/admin/v1/shutdown` is absent (plan 02-06 owes it); the shim installer is missing (plan 02-05); a Hermes plugin registration API named in `02-CONTEXT.md` does not exist; supervision cannot be made PID-reuse-proof without a dependency; verification fails after one focused fix.
</stop_gates>

<recovery>
1. Re-read this PLAN.md, `02-CONTEXT.md` § Supervision Contract, and all five prior summaries.
2. `git status --short`; inspect changed files. Confirm no orphaned sidecar is still running from a previous attempt — `status()` then `stop()`.
3. Any change outside `files_modified`, or any `pyproject.toml` change beyond the two entry-point blocks → `BLOCKED`.
4. Re-run verification for completed tasks; continue from the next unverified one.
</recovery>

<tasks>

<task type="auto">
  <name>Task 1: PID-reuse-proof supervision</name>
  <files>src/hermes_auto/supervisor.py</files>
  <action>
Read `02-CONTEXT.md` § Supervision Contract in full. It was frozen in Wave 1 precisely so this task implements rather than designs.

`start()` — spawn `subprocess.Popen([sys.executable, "-m", "hermes_auto.gateway.main"], ...)`. POSIX: `start_new_session=True`. Windows: `creationflags=CREATE_NEW_PROCESS_GROUP | DETACHED_PROCESS`, retrying with `CREATE_BREAKAWAY_FROM_JOB` if the spawn fails or the child dies immediately — CI runners commonly place the parent in a job object that kills detached children with it. Redirect stdout and stderr to a rotating file under the log dir; **never a pipe**, because nobody drains it and a full pipe deadlocks the sidecar. Poll `/healthz` until it answers or `startup_timeout_seconds` elapses.

`status()` — the heart of the plan. Never trust the PID:
1. `read_runtime()`. `None` → not running.
2. `GET http://127.0.0.1:{port}/healthz`.
3. Connection refused → stale. Remove the runtime file, report cleaned.
4. Answers with matching `instance_id` → running.
5. Answers with a **different** `instance_id` → another process owns that port. Report it, kill nothing.

Step 5 is why `instance_id` exists. A recycled PID belonging to an unrelated process cannot masquerade as a live gateway, and this check behaves identically on all three platforms without `psutil`.

`stop(timeout)` — escalate: `POST /admin/v1/shutdown` with the admin token (primary on every platform, because Windows has no `SIGTERM`) → wait for `/healthz` to stop answering → `Popen.terminate()` → `kill()` after the timeout. Clear the runtime file only after the port stops answering.

`restart()` — capture the current `instance_id`, `stop()`, `start()`, then wait for a **different** `instance_id`. Returning as soon as `/healthz` answers would pass against the old process during a slow drain.

> verification: test -f src/hermes_auto/supervisor.py
> verification: ./.venv/Scripts/python.exe -c "from hermes_auto.supervisor import start, stop, restart, status, Status, SupervisorError; print('interface OK')"
> verification: ./.venv/Scripts/python.exe -c "import inspect, hermes_auto.supervisor as s; src=inspect.getsource(s); assert 'psutil' not in src; assert 'instance_id' in src; assert 'shutdown' in src; assert 'DETACHED_PROCESS' in src or 'creationflags' in src"
> verification: ./.venv/Scripts/python.exe -c "import inspect, hermes_auto.supervisor as s; src=inspect.getsource(s); assert 'PIPE' not in src, 'stdout/stderr must go to a file, never a pipe'"
> verification: ./.venv/Scripts/python.exe -c "from hermes_auto.supervisor import status; st=status(); print('not-running path:', st.running, st.detail)"
  </action>
  <verify>
test -f src/hermes_auto/supervisor.py
./.venv/Scripts/python.exe -c "from hermes_auto.supervisor import start, stop, restart, status, Status, SupervisorError; print('interface OK')"
./.venv/Scripts/python.exe -c "import inspect, hermes_auto.supervisor as s; src=inspect.getsource(s); assert 'psutil' not in src; assert 'instance_id' in src; assert 'shutdown' in src; assert 'DETACHED_PROCESS' in src or 'creationflags' in src"
./.venv/Scripts/python.exe -c "import inspect, hermes_auto.supervisor as s; src=inspect.getsource(s); assert 'PIPE' not in src, 'stdout/stderr must go to a file, never a pipe'"
./.venv/Scripts/python.exe -c "from hermes_auto.supervisor import status; st=status(); print('not-running path:', st.running, st.detail)"
  </verify>
  <done>Supervision spawns detached with file-backed output, determines liveness by comparing the instance_id from /healthz against the runtime file rather than trusting a PID, stops via admin shutdown first, and restart waits for the identity to actually change.</done>
</task>

<task type="auto">
  <name>Task 2: The hermes auto CLI and console script</name>
  <files>src/hermes_auto/cli.py, src/hermes_auto/commands.py, pyproject.toml, tests/unit/test_cli.py</files>
  <action>
`commands.py` holds implementations: `cmd_setup`, `cmd_start`, `cmd_stop`, `cmd_restart`, `cmd_status`, `cmd_doctor`. Each returns an exit code and prints human-readable output. The control plugin calls these directly, so they must not depend on argparse.

`cmd_setup` — mint the inference and admin tokens if absent, call `hermes_shim.installer.install()`, **and enable the control plugin in Hermes's config**. That last step is not optional: `hermes_cli/plugins.py:1453` makes entry-point plugins opt-in via `plugins.enabled`, and when the key is absent `_get_enabled_plugins()` returns `None`, meaning nothing is enabled — `register()` is never called and `hermes auto ...` does not exist. Write `plugins.enabled += ["hermes-auto-control"]` into Hermes's `config.yaml`, or if that file cannot be safely edited, print the exact `hermes plugins enable hermes-auto-control` command and return non-zero. Record the bootstrap ordering in the output: on a first install the user reaches `hermes auto setup` through the console script in `[project.scripts]`, not through the plugin, because the plugin is not yet enabled. **State plainly in its output that this step is what registers the provider**, because model providers are discovered by directory scan and `pip install` alone never registers anything. A user who skips `setup` sees "unknown provider hermes-auto" with no clue why.

`cmd_doctor` — report, without stopping at the first failure:
- Hermes compatibility via `compatibility.check_compatibility()`
- Whether the shim is installed and whether its marker matches this version
- Whether the shim's envelope has drifted from `provider.py`'s (plan 02-05 records the duplication)
- Token file presence and **effective** permissions via `token_permissions_ok()` — read back, not assumed, since `os.chmod` is a no-op on Windows ACLs
- Admin token presence and permissions
- Gateway `status()`
- Upstream reachability via `/readyz` when running
- Config validity, with the offending key named on failure

`cli.py` — `build_parser()` with subcommands `setup`, `start`, `stop`, `restart`, `status`, `doctor`; `main(argv=None) -> int`.

`pyproject.toml` — add `[project.scripts] hermes-auto = "hermes_auto.cli:main"`. **This is the only pyproject change permitted in this plan.** No dependency edits.

Tests: parser exposes all six subcommands; `main(["status"])` returns 0 when not running; `main([])` prints help and returns non-zero; `doctor` returns non-zero when the shim is absent but still reports every other check rather than aborting on the first failure.

> verification: ./.venv/Scripts/python.exe -c "from hermes_auto.cli import build_parser; p=build_parser(); names=set(p._subparsers._group_actions[0].choices); need={'setup','start','stop','restart','status','doctor'}; assert need<=names, sorted(need-names)"
> verification: ./.venv/Scripts/python.exe -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); s=d['project']['scripts']; assert 'hermes-auto' in s and 'cli:main' in s['hermes-auto'], s"
> verification: ./.venv/Scripts/python.exe -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); deps=' '.join(d['project']['dependencies']); assert 'psutil' not in deps, 'psutil must not be added'"
> verification: ./.venv/Scripts/python.exe -c "from hermes_auto.cli import main; assert main(['status'])==0"
> verification: ./.venv/Scripts/python.exe -m pytest tests/unit/test_cli.py -q
  </action>
  <verify>
./.venv/Scripts/python.exe -c "from hermes_auto.cli import build_parser; p=build_parser(); names=set(p._subparsers._group_actions[0].choices); need={'setup','start','stop','restart','status','doctor'}; assert need<=names, sorted(need-names)"
./.venv/Scripts/python.exe -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); s=d['project']['scripts']; assert 'hermes-auto' in s and 'cli:main' in s['hermes-auto'], s"
./.venv/Scripts/python.exe -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); deps=' '.join(d['project']['dependencies']); assert 'psutil' not in deps"
./.venv/Scripts/python.exe -c "from hermes_auto.cli import main; assert main(['status'])==0"
./.venv/Scripts/python.exe -m pytest tests/unit/test_cli.py -q
  </verify>
  <done>Six subcommands are exposed through a console script, setup installs the shim and says why that step is required, and doctor reports every check including a read-back of effective token permissions rather than stopping at the first failure.</done>
</task>

<task type="auto">
  <name>Task 3: The Hermes control plugin and supervision integration tests</name>
  <files>src/hermes_auto/plugin.py, pyproject.toml (entry point), tests/integration/test_supervisor.py</files>
  <action>
`plugin.py` exports `register(ctx)` and registers, per the surface recorded in `02-CONTEXT.md`:
- `ctx.register_cli_command` for `auto`, dispatching to `commands.py`
- `ctx.register_command` for `/auto status` **only** — `mode`, `explain`, `candidates`, `pin`, `unpin`, `reroute`, `feedback`, and `stats` all describe routing that does not exist. Registering a command that reports fabricated state is worse than not registering it.
- `ctx.register_hook("on_session_start", ...)` — the literal hook name, from `VALID_HOOKS` at `hermes_cli/plugins.py:135`. `register_hook` accepts an unknown name with only a `logger.warning` and stores it where it will never fire, so a wrong name fails silently. On session start: if `gateway.auto_start` is true and the gateway is not running, start it. Failure here **must not** break the session — log through the redaction sink and continue, because a router that cannot start should degrade to an error on the next request, not prevent Hermes from opening.

Add `[project.entry-points."hermes_agent.plugins"]` naming this module. Note in a comment that this entry point registers the **control** plugin only; the **provider** is registered by the directory-scan shim, and conflating the two is the mistake `design.md` §5.1 invites.

`tests/integration/test_supervisor.py` — real process lifecycle against the mock upstream:
- `start()` → `status().running` is True with a non-null `instance_id`
- `start()` twice is a no-op returning the same `instance_id`
- `stop()` → not running, runtime file cleared
- `restart()` → `instance_id` **changes**
- Stale runtime file (write one for a dead port) → `status()` reports not running and removes it
- Foreign process on the recorded port → `status()` reports the mismatch and kills nothing
- `stop()` while a stream is in flight completes the stream rather than severing it
- After `stop()`, no orphan process holds the port — rebind it to prove release
- stdout/stderr landed in the log file, and the log contains **no** raw prompt content from the streamed request

Mark the whole module so it can be deselected in constrained CI, and ensure teardown always stops the sidecar even on failure.

> verification: ./.venv/Scripts/python.exe -c "from hermes_auto.plugin import register; print('register present')"
> verification: ./.venv/Scripts/python.exe -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); ep=d['project']['entry-points']['hermes_agent.plugins']; assert ep, ep"
> verification: ./.venv/Scripts/python.exe -c "import inspect, hermes_auto.plugin as p; s=inspect.getsource(p); assert '/auto status' in s or 'status' in s; assert 'on_session_start' in s, 'hook name must be literal'"
> verification: ./.venv/Scripts/python.exe -m pytest tests/integration/test_supervisor.py -q
> verification: ./.venv/Scripts/python.exe -m pytest tests/ -q
  </action>
  <verify>
./.venv/Scripts/python.exe -c "from hermes_auto.plugin import register; print('register present')"
./.venv/Scripts/python.exe -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); ep=d['project']['entry-points']['hermes_agent.plugins']; assert ep, ep"
./.venv/Scripts/python.exe -c "import inspect, hermes_auto.plugin as p; s=inspect.getsource(p); assert 'reroute' not in s and 'feedback' not in s, 'do not register commands for routing that does not exist'"
./.venv/Scripts/python.exe -m pytest tests/integration/test_supervisor.py -q
./.venv/Scripts/python.exe -m pytest tests/ -q
  </verify>
  <done>The control plugin registers the CLI surface, only the slash command backed by real behavior, and a session hook that fails soft; integration tests prove restart changes instance_id, a stale runtime file is cleaned, a foreign port holder is reported rather than killed, and the log carries no prompt content.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `status()` compares `instance_id` from `/healthz` against the runtime file; no PID trust anywhere
- [ ] A foreign process on the recorded port is reported, not killed
- [ ] `stop()` tries admin shutdown first on every platform
- [ ] `restart()` waits for `instance_id` to change
- [ ] Spawn is detached with file-backed output, never a pipe; Windows has the job-object fallback
- [ ] No `psutil` and no other new dependency
- [ ] No systemd unit, launchd plist, or Windows Service
- [ ] Only `/auto status` is registered as a slash command
- [ ] The session hook fails soft
- [ ] `setup` installs the shim and explains why that step is required
- [ ] `doctor` reads back effective token permissions and reports all checks
- [ ] `pyproject.toml` changed only for `[project.scripts]` and the plugin entry point
- [ ] The sidecar log contains no raw prompt content
- [ ] No orphan holds the port after `stop()`
- [ ] No `gateway/` module modified
- [ ] `design.md` unchanged and matches its pinned blob
- [ ] Every planned task is completed, blocked with evidence, or escalated
</verification>

<success_criteria>
- `./.venv/Scripts/python.exe -m pytest tests/ -q` exits 0
- A real start/stop/restart cycle completes with `instance_id` changing across the restart
- A stale runtime file is detected and cleaned without killing anything
- `git status --short` shows no change to `src/hermes_auto/gateway/`, `state/`, `config.py`, `telemetry/`, `provider.py`, `hermes_shim/`, `tests/contract/`, `docs/`, or `design.md`
</success_criteria>

<output>
After completion, create `.planning/phases/02-provider-plugin-passthrough-gateway/02-07-SUMMARY.md` recording: the platform this ran on and which Windows creation-flag path was taken; measured start and stop latency; the `doctor` check list and its output on a healthy install; whether the job-object fallback was needed; and any `design.md` §4.2 CLI or slash command deliberately not registered, with the phase that will deliver it.
</output>
