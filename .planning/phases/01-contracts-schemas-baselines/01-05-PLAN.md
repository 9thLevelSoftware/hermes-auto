---
phase: 01-contracts-schemas-baselines
plan: 05
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/hermes_auto/compatibility.py
  - tests/unit/test_compatibility.py
  - .github/workflows/ci.yml
  - .github/workflows/hermes-compat.yml
files_forbidden:
  - design.md
  - .planning/
  - pyproject.toml
  - docs/
  - scripts/
  - src/hermes_auto/data/
  - src/hermes_auto/gateway/
  - src/hermes_auto/routing/
  - tests/conftest.py
  - tests/contract/
sequential_files: []
expected_artifacts:
  - path: "src/hermes_auto/compatibility.py"
    provides: "Supported Hermes version range and a fail-closed compatibility probe"
    required: true
  - path: "tests/unit/test_compatibility.py"
    provides: "Unit tests covering in-range, out-of-range, absent, and unparseable Hermes versions"
    required: true
  - path: ".github/workflows/ci.yml"
    provides: "Main CI workflow running the full test suite across three operating systems, plus a schema-validation job"
    required: true
  - path: ".github/workflows/hermes-compat.yml"
    provides: "Nightly compatibility workflow against the latest Hermes release and current main"
    required: true
autonomous: false
agents: [engineering-infrastructure-devops, testing-workflow-optimizer]
requirements: [R26, R25]
user_setup: []
verification_commands:
  - "test -f src/hermes_auto/compatibility.py && test -f tests/unit/test_compatibility.py"
  - "test -f .github/workflows/ci.yml && test -f .github/workflows/hermes-compat.yml"
  - "python -c \"import yaml,sys; [yaml.safe_load(open(f)) for f in ['.github/workflows/ci.yml','.github/workflows/hermes-compat.yml']]\""
  - "PYTHONPATH=src python -c \"from hermes_auto.compatibility import SUPPORTED_HERMES, check_compatibility, CompatibilityStatus; assert SUPPORTED_HERMES\""
  - "python -m pytest tests/unit/test_compatibility.py -q"
  - "python -c \"import yaml; w=yaml.safe_load(open('.github/workflows/ci.yml')); os=w['jobs']['test']['strategy']['matrix']['os']; assert len(os)==3, os\""
  - "python -c \"import yaml; w=yaml.safe_load(open('.github/workflows/hermes-compat.yml')); t=w.get('on', w.get(True)); assert 'schedule' in t and 'workflow_dispatch' in t, t\""
  - "python -c \"import yaml; w=yaml.safe_load(open('.github/workflows/ci.yml')); assert 'schema-validation' in w['jobs'], sorted(w['jobs']); assert w['permissions']['contents']=='read'; assert w['concurrency']['cancel-in-progress'] is True\""
  - "PYTHONPATH=src python -c \"from hermes_auto.compatibility import HERMES_DISTRIBUTION_NAME as n, SUPPORTED_HERMES as s; assert n=='hermes-agent', n; assert s=='>=0.19,<1.0', s\""
  - "grep -riE '(^|[^a-z])(ruff|flake8|black|mypy)([^a-z]|$)' .github/workflows/ci.yml && exit 1 || exit 0"

must_haves:
  truths:
    - "check_compatibility() fails closed: an absent, unparseable, or out-of-range Hermes version returns an explicit non-compatible status rather than assuming compatibility."
    - "HERMES_DISTRIBUTION_NAME is 'hermes-agent' — the name verified installed on this machine. 'hermes' is not a Hermes distribution and would make the probe report HERMES_NOT_INSTALLED forever."
    - "SUPPORTED_HERMES is '>=0.19,<1.0', derived from the installed hermes-agent 0.19.0 baseline rather than invented, with the derivation recorded in the summary."
    - "The supported version range is declared as data in one place, so widening support is a one-line change plus a test update."
    - "compatibility.py imports nothing from hermes_auto beyond the standard library and packaging, so it can run before any other subsystem initializes."
    - "The main CI workflow runs the full test suite on ubuntu-latest, macos-latest, and windows-latest, matching the three platforms the sidecar must support."
    - "The nightly workflow tests against both the latest Hermes release and current main, and a main-only failure does not fail the release-tracking job."
    - "No workflow contains a hardcoded credential, token, or registry password."
  artifacts:
    - path: "src/hermes_auto/compatibility.py"
      provides: "Version range and fail-closed probe"
      min_lines: 45
      contains: "def check_compatibility"
    - path: ".github/workflows/ci.yml"
      provides: "Cross-platform CI"
      min_lines: 30
      contains: "windows-latest"
    - path: ".github/workflows/hermes-compat.yml"
      provides: "Nightly Hermes compatibility matrix"
      min_lines: 30
      contains: "schedule"
  key_links:
    - from: "tests/unit/test_compatibility.py"
      to: "src/hermes_auto/compatibility.py"
      via: "direct import of the probe and status type"
      pattern: "from hermes_auto.compatibility import"
    - from: ".github/workflows/ci.yml"
      to: "pyproject.toml"
      via: "editable install of the dev extra"
      pattern: "\\[dev\\]"
---

<objective>
Pin the supported Hermes version range behind a fail-closed compatibility probe, and stand up the two CI workflows: the main cross-platform test workflow and the nightly workflow that tracks both the latest Hermes release and current `main`.

Purpose: design.md §18 Phase 0 requires pinning supported Hermes versions and establishing CI against both the latest release and current `main` before any integration code exists. The probe is the runtime half of that contract — the optional Hermes-native bridge in Phase 7 and the provider plugin in Phase 2 both depend on being able to ask "is this Hermes supported?" and get a definite answer rather than an optimistic one.
Output: `src/hermes_auto/compatibility.py`, its unit tests, and two GitHub Actions workflows.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-contracts-schemas-baselines/01-CONTEXT.md
@.planning/phases/01-contracts-schemas-baselines/01-01-SUMMARY.md

Relevant source files:
@design.md — read §18 Phase 0 item 3 (pin supported Hermes versions, CI against latest release and current main), §10.2 Tier 4 (the version-gated credential bridge with a startup compatibility probe, clear fail-closed behavior, and nightly CI against Hermes main), §11.3 (sidecar outage layers). Read-only.
@pyproject.toml — read only, to confirm `packaging` is already a declared dependency and to reuse the dev extra name. Do not modify.
</context>

<execution_contract>
Harness: read-before-write -> evidence-before-action -> minimal diff -> verify-before-report

Role: `engineering-infrastructure-devops` is the primary executor and owns both workflow files and the CI matrix design. `testing-workflow-optimizer` owns the test-job structure, caching strategy, and the `compatibility.py` unit test suite.
Task: Deliver the version-pinning and CI surface of R26, plus the nightly-compatibility-CI portion of R25. This plan introduces no routing behavior and no Hermes imports.
Scope:
- Read targets: `design.md` §18 Phase 0, §10.2 Tier 4, §11.3; `pyproject.toml`; `.planning/phases/01-contracts-schemas-baselines/01-01-SUMMARY.md`.
- Write targets: exactly the four paths in `files_modified`. This plan creates `.github/workflows/`.
- Forbidden targets: `design.md`, `.planning/`, `pyproject.toml`, `docs/`, `scripts/`, `src/hermes_auto/data/`, `src/hermes_auto/gateway/`, `src/hermes_auto/routing/`, `tests/conftest.py`, `tests/contract/` (the last four are owned by plans 01-03 and 01-04 running in parallel in this wave).
Allowed tools/actions:
- Read `design.md`, `pyproject.toml`, and the planning files.
- Create the four files in `files_modified`.
- Run `python -c`, `python -m pytest`, and YAML parsing for verification.
Forbidden actions:
- Do not modify files outside `files_modified`.
- Do not edit `pyproject.toml`; `packaging` and `pyyaml` are already declared by plan 01-01. If you conclude another dependency is needed, emit `BLOCKED`.
- Do not `import hermes` or add Hermes as a dependency. The probe inspects an installed Hermes if present but must import cleanly when Hermes is absent.
- Do not create anything under `src/hermes_auto/gateway/`, `src/hermes_auto/data/`, or `tests/contract/` — plans 01-03 and 01-04 own those and run in parallel.
- Do not write a publish, release, or deploy workflow. Packaging and release automation are Phase 11 (R25) work; this plan delivers only the two workflows named above.
- Do not put any credential, token, or password literal in a workflow file.
- Do not self-defer planned work.
Implementation sequence:
1. Confirm plan 01-01 delivered `src/hermes_auto/__init__.py`, the `tests/unit/` directory, and the `packaging` dependency. If any is missing, emit `BLOCKED`.
2. Write `compatibility.py` (Task 1).
3. Write its unit tests (Task 2).
4. Write the two workflows (Task 3).
**Resolved facts — do not re-derive, do not guess.** Verified against the upstream repository and PyPI during planning:
- Hermes lives at **`https://github.com/NousResearch/hermes-agent`** (MIT, default branch `main`).
- The distribution name is **`hermes-agent`**. `importlib.metadata.version("hermes")` raises `PackageNotFoundError`; `importlib.metadata.version("hermes-agent")` returns `0.19.0`.
- The version `0.19.0` is consistent across three independent sources: the repository's `pyproject.toml` (`version = "0.19.0"`), PyPI, and the local install.
- `hermes-agent` **is published on PyPI**, so `pip install hermes-agent` works in CI.
- Hermes declares `requires-python = ">=3.11,<3.14"`.
- The supported range is **`">=0.19,<1.0"`**. The lower bound is now evidence-based, not invented — it is the upstream declared version. design.md still gives no bounds anywhere (§10.2 says only "explicit supported-version range"; §18 Phase 0 item 3 says only "pin supported Hermes versions"), so the *upper* bound remains a deliberate project decision and the summary must say so.
- Repository variables `HERMES_GIT_URL` and `HERMES_DIST_NAME` were **provisioned on 2026-07-27** and are available to both workflows.

**Version-scheme trap.** Hermes's git *tags* are CalVer — `v2026.7.20`, `v2026.7.7.2`, `v2026.6.19`. Its *distribution version* is semver `0.19.0`. The probe compares the distribution version, which `importlib.metadata` reports as `0.19.0` and never as `2026.7.20`. If you look at the repository's releases page and conclude `SUPPORTED_HERMES` should be CalVer, you are wrong: a `>=2026.1` specifier would match nothing and make the probe report `UNSUPPORTED_VERSION` for every real Hermes install. Do not change the range to CalVer. Add a comment in `compatibility.py` recording this so the next reader does not repeat the mistake.

Required interfaces/content structure:
- `src/hermes_auto/compatibility.py` exports exactly these public names:
  - `SUPPORTED_HERMES: str` — the PEP 440 specifier `">=0.19,<1.0"`.
  - `class CompatibilityStatus(enum.Enum)` with members `COMPATIBLE`, `UNSUPPORTED_VERSION`, `HERMES_NOT_INSTALLED`, `VERSION_UNREADABLE`.
  - `@dataclasses.dataclass(frozen=True) class CompatibilityResult` with fields `status: CompatibilityStatus`, `detected_version: str | None`, `supported_range: str`, `detail: str`.
  - `def detect_hermes_version() -> str | None` — returns the installed Hermes version via `importlib.metadata.version("hermes")`, or `None` when the distribution is absent. It must catch `importlib.metadata.PackageNotFoundError` only; other exceptions propagate.
  - `def check_compatibility(version: str | None = None) -> CompatibilityResult` — when `version` is `None`, calls `detect_hermes_version()`. Returns a result whose `status` is `COMPATIBLE` only when a version is present, parses, and satisfies `SUPPORTED_HERMES`.
  - `__all__` listing all of the above.
- Both workflows use `actions/checkout@v4` and `actions/setup-python@v5` pinned by major version tag.
Edge/error cases:
- Hermes not installed: status `HERMES_NOT_INSTALLED`, `detected_version` None, `detail` naming the supported range. This is NOT an error — the gateway runs standalone; it is information for `hermes auto doctor` in Phase 2.
- Version present but unparseable by `packaging.version.Version`: status `VERSION_UNREADABLE`. Never fall through to `COMPATIBLE`.
- Version parses but falls outside the specifier: status `UNSUPPORTED_VERSION`.
- Pre-release versions: use `packaging.specifiers.SpecifierSet(SUPPORTED_HERMES).contains(v, prereleases=True)` so a Hermes pre-release inside the range is treated as in-range rather than silently excluded.
- The nightly `main` job must use `continue-on-error: true` so a break in unreleased Hermes reports without failing the whole workflow run; the release-tracking job must not set it.
Verification criteria:
- All seven commands in `verification_commands` exit 0.
- `python -m pytest tests/unit/test_compatibility.py -q` passes with Hermes absent from the environment.
Final result format:
- Status: Complete | Partial | Failed | BLOCKED
- Files changed
- Verification commands and outputs
- Decisions made
- Issues/errors
</execution_contract>

<stop_gates>
Emit `BLOCKED` and stop instead of guessing when:
- Plan 01-01's outputs are missing: `src/hermes_auto/__init__.py`, `tests/unit/`, or the `packaging` dependency in `pyproject.toml`.
- `importlib.metadata.version("hermes-agent")` raises `PackageNotFoundError` on this machine, contradicting the resolved fact above — report the discrepancy rather than substituting another name.
- Completing the task requires editing `pyproject.toml` or adding a dependency, including adding a linter to satisfy any description of `ci.yml`.
- Completing the task requires importing Hermes at module scope.
- A workflow needs a repository secret that has not been provisioned; name the secret in the blocker rather than inlining a placeholder credential.
- Verification commands fail after one focused fix attempt.
</stop_gates>

<recovery>
After compaction, interruption, or context loss:
1. Re-read this PLAN.md, `01-CONTEXT.md`, `01-01-SUMMARY.md`, and any `01-05-SUMMARY.md` already written.
2. Run `git status --short` and `git diff --stat`; inspect `git diff` for every file already changed.
3. Compare changed files against `files_modified` and `files_forbidden`; if `pyproject.toml` or anything under `tests/contract/` was modified, emit `BLOCKED`.
4. Re-run `python -m pytest tests/unit/test_compatibility.py -q` for work already marked done.
5. Continue only from the next unverified task; do not rely on memory-only claims.
</recovery>

<tasks>

<task type="auto">
  <name>Task 1: Write the fail-closed compatibility probe</name>
  <files>src/hermes_auto/compatibility.py</files>
  <action>
Confirm prerequisites: `test -f src/hermes_auto/__init__.py`, `test -d tests/unit`, and `grep -q packaging pyproject.toml`. If any check fails, emit `BLOCKED`.

Read `design.md` §10.2 Tier 4 for the fail-closed requirement and §18 Phase 0 item 3 for the pinning requirement.

Create `src/hermes_auto/compatibility.py` with a module docstring stating that this module must be importable with Hermes absent and must never assume compatibility it has not verified.

Declare the version range as a single module-level constant, with the CalVer warning inline:
```python
# Hermes upstream: https://github.com/NousResearch/hermes-agent
#
# The lower bound is the version NousResearch/hermes-agent declares in its own
# pyproject.toml (0.19.0), confirmed identical on PyPI and in the local install.
# The upper bound is a project decision: design.md states no version bounds.
#
# WARNING: Hermes's git TAGS are CalVer (v2026.7.20, v2026.7.7.2, ...) but its
# DISTRIBUTION version is semver 0.19.0. importlib.metadata reports 0.19.0, never
# 2026.7.20. Do not "correct" this range to CalVer after reading the releases page
# — a >=2026.1 specifier matches nothing and would report UNSUPPORTED_VERSION for
# every real Hermes install.
#
# Widening this range requires updating tests/unit/test_compatibility.py and a
# CHANGELOG entry.
SUPPORTED_HERMES: str = ">=0.19,<1.0"
```

Declare the distribution name as a separate constant:
```python
HERMES_DISTRIBUTION_NAME: str = "hermes-agent"
```
This name is verified against three sources, not guessed: the upstream `pyproject.toml` declares `name = "hermes-agent"`, PyPI publishes `hermes-agent==0.19.0`, and `importlib.metadata.version("hermes-agent")` returns `0.19.0` locally while `importlib.metadata.version("hermes")` raises `PackageNotFoundError`. Do not change it to `hermes`. Confirm once before writing by running `python -c "import importlib.metadata as m; print(m.version('hermes-agent'))"`; if that raises, emit `BLOCKED` reporting the discrepancy.

Define `class CompatibilityStatus(enum.Enum)` with exactly four members: `COMPATIBLE`, `UNSUPPORTED_VERSION`, `HERMES_NOT_INSTALLED`, `VERSION_UNREADABLE`. Use string values equal to the lowercase member name so the status serializes readably into a future `hermes auto doctor` JSON output.

Define `CompatibilityResult` as a frozen dataclass with `status`, `detected_version: str | None`, `supported_range: str`, and `detail: str`. Add a `is_usable` property returning `True` only for `COMPATIBLE`. Do not treat `HERMES_NOT_INSTALLED` as usable — the gateway runs standalone, but the *bridge* is not usable, and this type answers the bridge question.

Implement `detect_hermes_version() -> str | None` using `importlib.metadata.version(HERMES_DISTRIBUTION_NAME)`, catching only `importlib.metadata.PackageNotFoundError` and returning `None`. Do not catch broad `Exception` — an unexpected metadata error should surface, not be silently converted to "not installed".

Implement `check_compatibility(version: str | None = None) -> CompatibilityResult`:
- If `version` is None, call `detect_hermes_version()`.
- If still None: return `HERMES_NOT_INSTALLED` with a detail naming `SUPPORTED_HERMES`.
- Try `packaging.version.Version(version)`. On `packaging.version.InvalidVersion`, return `VERSION_UNREADABLE` with a detail quoting the raw string.
- Build `packaging.specifiers.SpecifierSet(SUPPORTED_HERMES)` and call `.contains(parsed, prereleases=True)`.
- Return `COMPATIBLE` on a match, otherwise `UNSUPPORTED_VERSION` with a detail naming both the detected version and the supported range.

There must be no code path that returns `COMPATIBLE` without a successful parse and specifier match. Do not add a "assume compatible if unsure" branch, an override environment variable, or a permissive fallback.

Add `__all__ = ["SUPPORTED_HERMES", "HERMES_DISTRIBUTION_NAME", "CompatibilityStatus", "CompatibilityResult", "detect_hermes_version", "check_compatibility"]`.

Do not import anything from `hermes` at module scope or inside any function. Do not import from other `hermes_auto` submodules — this module must be loadable before anything else initializes.

> verification: test -f src/hermes_auto/compatibility.py
> verification: grep -q 'SUPPORTED_HERMES' src/hermes_auto/compatibility.py
> verification: grep -q 'def check_compatibility' src/hermes_auto/compatibility.py
> verification: grep -q 'def detect_hermes_version' src/hermes_auto/compatibility.py
> verification: grep -q 'PackageNotFoundError' src/hermes_auto/compatibility.py
> verification: grep -q 'prereleases=True' src/hermes_auto/compatibility.py
> verification: grep -qE '^\s*import hermes\b|^\s*from hermes\b' src/hermes_auto/compatibility.py && exit 1 || exit 0
> verification: PYTHONPATH=src python -c "from hermes_auto.compatibility import HERMES_DISTRIBUTION_NAME as n, SUPPORTED_HERMES as s; assert n=='hermes-agent', n; assert s=='>=0.19,<1.0', s"
> verification: PYTHONPATH=src python -c "from hermes_auto.compatibility import check_compatibility, CompatibilityStatus as S; r=check_compatibility('0.19.0'); assert r.status is S.COMPATIBLE, r; r=check_compatibility('0.18.0'); assert r.status is S.UNSUPPORTED_VERSION, r; r=check_compatibility('2.0.0'); assert r.status is S.UNSUPPORTED_VERSION, r; r=check_compatibility('not-a-version'); assert r.status is S.VERSION_UNREADABLE, r"
> verification: PYTHONPATH=src python -c "from hermes_auto.compatibility import detect_hermes_version; assert detect_hermes_version() is not None, 'hermes-agent should be detectable on this machine'"
  </action>
  <verify>
test -f src/hermes_auto/compatibility.py
grep -q 'SUPPORTED_HERMES' src/hermes_auto/compatibility.py
grep -q 'def check_compatibility' src/hermes_auto/compatibility.py
grep -q 'def detect_hermes_version' src/hermes_auto/compatibility.py
grep -q 'PackageNotFoundError' src/hermes_auto/compatibility.py
grep -q 'prereleases=True' src/hermes_auto/compatibility.py
grep -qE '^\s*import hermes\b|^\s*from hermes\b' src/hermes_auto/compatibility.py && exit 1 || exit 0
PYTHONPATH=src python -c "from hermes_auto.compatibility import HERMES_DISTRIBUTION_NAME as n, SUPPORTED_HERMES as s; assert n=='hermes-agent', n; assert s=='>=0.19,<1.0', s"
PYTHONPATH=src python -c "from hermes_auto.compatibility import check_compatibility, CompatibilityStatus as S; r=check_compatibility('0.19.0'); assert r.status is S.COMPATIBLE, r; r=check_compatibility('0.18.0'); assert r.status is S.UNSUPPORTED_VERSION, r; r=check_compatibility('2.0.0'); assert r.status is S.UNSUPPORTED_VERSION, r; r=check_compatibility('not-a-version'); assert r.status is S.VERSION_UNREADABLE, r"
PYTHONPATH=src python -c "from hermes_auto.compatibility import detect_hermes_version; assert detect_hermes_version() is not None, 'hermes-agent should be detectable on this machine'"
  </verify>
  <done>compatibility.py declares the supported Hermes range in one place, imports cleanly with Hermes absent, and returns COMPATIBLE only after a successful version parse and specifier match — with no permissive fallback path.</done>
</task>

<task type="auto">
  <name>Task 2: Write compatibility unit tests</name>
  <files>tests/unit/test_compatibility.py</files>
  <action>
Create `tests/unit/test_compatibility.py` importing from `hermes_auto.compatibility`.

Write these tests:

- `test_in_range_version_is_compatible` — parametrize over `"0.19.0"` (the installed baseline, so this test also documents it), `"0.25.1"`, and `"0.99.9"`, asserting `status is CompatibilityStatus.COMPATIBLE` and `is_usable` is True.
- `test_out_of_range_version_is_unsupported` — parametrize over `"0.18.9"` (just below the floor) and `"1.0.0"` (the ceiling, exclusive), asserting `UNSUPPORTED_VERSION`. Assert the `detail` string contains both the detected version and the supported range, so an operator reading a doctor report can act on it.
- `test_unparseable_version_is_unreadable` — parametrize over `"not-a-version"`, `""`, and `"latest"`, asserting `VERSION_UNREADABLE` in every case. This is the fail-closed test: assert `is_usable` is False for each.
- `test_prerelease_inside_range_is_compatible` — pass `"0.20.0rc1"` and assert `COMPATIBLE`, guarding the `prereleases=True` behavior.
- `test_distribution_name_is_hermes_agent` — assert `HERMES_DISTRIBUTION_NAME == "hermes-agent"`. This is the regression guard for the corrected name: `"hermes"` is not a Hermes distribution and would make the probe permanently report HERMES_NOT_INSTALLED.
- `test_calver_tag_is_not_treated_as_a_version` — pass `"2026.7.20"` (a real Hermes git tag) and assert `UNSUPPORTED_VERSION`, not `COMPATIBLE`. Add a docstring explaining that Hermes tags are CalVer while its distribution version is semver, and that this test exists so a future maintainer who "fixes" `SUPPORTED_HERMES` to a CalVer range breaks a test with an explanation attached rather than silently disabling the probe.
- `test_detect_reads_the_configured_distribution_name(monkeypatch)` — monkeypatch `importlib.metadata.version` with a recorder, call `detect_hermes_version()`, and assert it was called with exactly `HERMES_DISTRIBUTION_NAME`. This proves the constant is actually the indirection point rather than a decorative duplicate of a hardcoded string.
- `test_missing_hermes_reports_not_installed(monkeypatch)` — monkeypatch `hermes_auto.compatibility.detect_hermes_version` to return `None`, call `check_compatibility()` with no argument, and assert `HERMES_NOT_INSTALLED` with `detected_version is None` and `is_usable` False.
- `test_detect_returns_none_when_distribution_absent(monkeypatch)` — monkeypatch `importlib.metadata.version` to raise `PackageNotFoundError`, and assert `detect_hermes_version()` returns `None`.
- `test_no_status_other_than_compatible_is_usable` — iterate every `CompatibilityStatus` member, construct a `CompatibilityResult` with it, and assert `is_usable` is True only for `COMPATIBLE`. This test fails if someone later adds a permissive status.
- `test_supported_range_is_a_valid_specifier` — assert `packaging.specifiers.SpecifierSet(SUPPORTED_HERMES)` constructs without error, so a typo in the constant is caught immediately.

Use `pytest.mark.parametrize` where the test description says parametrize. Do not use network access, do not require Hermes to be installed, and do not skip any test conditionally on Hermes presence — every test here must run in a clean environment.

Run `python -m pytest tests/unit/test_compatibility.py -q` and confirm all pass.

> verification: test -f tests/unit/test_compatibility.py
> verification: grep -q 'from hermes_auto.compatibility import' tests/unit/test_compatibility.py
> verification: grep -q 'test_no_status_other_than_compatible_is_usable' tests/unit/test_compatibility.py
> verification: grep -q 'test_prerelease_inside_range_is_compatible' tests/unit/test_compatibility.py
> verification: grep -q 'test_distribution_name_is_hermes_agent' tests/unit/test_compatibility.py
> verification: grep -q 'test_calver_tag_is_not_treated_as_a_version' tests/unit/test_compatibility.py
> verification: grep -q 'test_detect_reads_the_configured_distribution_name' tests/unit/test_compatibility.py
> verification: PYTHONPATH=src python -c "from hermes_auto.compatibility import check_compatibility, CompatibilityStatus as S; assert check_compatibility('2026.7.20').status is S.UNSUPPORTED_VERSION"
> verification: grep -q 'NousResearch/hermes-agent' src/hermes_auto/compatibility.py
> verification: grep -q 'monkeypatch' tests/unit/test_compatibility.py
> verification: grep -qi 'skipif' tests/unit/test_compatibility.py && exit 1 || exit 0
> verification: python -m pytest tests/unit/test_compatibility.py -q
  </action>
  <verify>
test -f tests/unit/test_compatibility.py
grep -q 'from hermes_auto.compatibility import' tests/unit/test_compatibility.py
grep -q 'test_no_status_other_than_compatible_is_usable' tests/unit/test_compatibility.py
grep -q 'test_prerelease_inside_range_is_compatible' tests/unit/test_compatibility.py
grep -q 'test_distribution_name_is_hermes_agent' tests/unit/test_compatibility.py
grep -q 'test_calver_tag_is_not_treated_as_a_version' tests/unit/test_compatibility.py
grep -q 'test_detect_reads_the_configured_distribution_name' tests/unit/test_compatibility.py
PYTHONPATH=src python -c "from hermes_auto.compatibility import check_compatibility, CompatibilityStatus as S; assert check_compatibility('2026.7.20').status is S.UNSUPPORTED_VERSION"
grep -q 'NousResearch/hermes-agent' src/hermes_auto/compatibility.py
grep -q 'monkeypatch' tests/unit/test_compatibility.py
grep -qi 'skipif' tests/unit/test_compatibility.py && exit 1 || exit 0
python -m pytest tests/unit/test_compatibility.py -q
  </verify>
  <done>Unit tests cover in-range, out-of-range, unparseable, pre-release, and absent-distribution cases, and assert that no status other than COMPATIBLE is usable — all running without Hermes installed and without conditional skips.</done>
</task>

<task type="auto">
  <name>Task 3: Write the main CI and nightly Hermes compatibility workflows</name>
  <files>.github/workflows/ci.yml, .github/workflows/hermes-compat.yml</files>
  <action>
Read `design.md` §18 Phase 0 item 3 and §10.2 Tier 4 for the nightly-CI requirement.

**`.github/workflows/ci.yml`** — the main workflow.

- `name: CI`
- Triggers: `push` on branches `[main]`, and `pull_request` on branches `[main]`.
- Add a top-level `permissions: contents: read` block — least privilege, since this workflow only reads the repository.
- Add `concurrency` with `group: ci-${{ github.ref }}` and `cancel-in-progress: true` so superseded PR pushes do not queue.
- Job `test`:
  - `strategy.fail-fast: false` and `strategy.matrix.os: [ubuntu-latest, macos-latest, windows-latest]` — exactly the three platforms the sidecar must supervise per PROJECT.md § Constraints.
  - `strategy.matrix.python-version: ["3.11", "3.12"]`.
  - `runs-on: ${{ matrix.os }}`.
  - Steps: `actions/checkout@v4`; `actions/setup-python@v5` with `python-version: ${{ matrix.python-version }}` and `cache: pip`; a step running `python -m pip install --upgrade pip` then `python -m pip install -e ".[dev]"`; a step running `python -m pytest tests/ -q`.
  - Do **not** add a lint job or a linter dependency. Lint tooling arrives in Phase 11 per R25, and adding one here would require editing `pyproject.toml`, which this plan forbids. The `expected_artifacts` description has been corrected to match.
  - Do not add a coverage gate, a codecov upload, or an artifact upload — those are Phase 11 concerns and would need secrets this repository has not provisioned.
- Job `schema-validation`:
  - `runs-on: ubuntu-latest`, needs nothing.
  - Steps: checkout; setup-python 3.12; install the dev extra; then exactly one unconditional step running:
    `python -c "from hermes_auto.gateway.schemas import load_schemas; s = load_schemas(); assert len(s) >= 7, sorted(s)"`
  - No `continue-on-error`, and no tolerance clause of any kind. An empty or short result is a failure.
  - This job asserts the *phase-complete* state (all seven schemas from plans 01-03 and 01-04). It is expected to fail on any commit made before both of those plans land. That is correct and intended: it is a phase-close gate that happens to live in CI, not a wave-2 gate. GitHub Actions evaluates it against a committed tree, never against mid-wave working state, so it creates no ordering hazard for the parallel plans.

**`.github/workflows/hermes-compat.yml`** — the nightly compatibility workflow.

- `name: Hermes Compatibility`
- Triggers: `schedule` with `cron: "0 6 * * *"` (06:00 UTC daily), plus `workflow_dispatch` so it can be run on demand.
- `permissions: contents: read`.
- Job `latest-release`:
  - `runs-on: ubuntu-latest`.
  - Steps: checkout; setup-python 3.12; install the dev extra.
  - Then a Hermes install step gated on the repository variable: install `${{ vars.HERMES_DIST_NAME }}` when non-empty, otherwise print an explicit skip message naming the unprovisioned variable and exit 0. Do NOT hardcode `pip install hermes-agent`. `hermes-agent` **is** published on PyPI at 0.19.0 and `HERMES_DIST_NAME` is already provisioned, so this step will genuinely install and this job will genuinely measure — the variable indirection stays because the distribution name is upstream-controlled and a rename must be fixable without editing the workflow.
  - Then a step running the compatibility probe via `python -c`, printing the `CompatibilityResult` and asserting the status is not `VERSION_UNREADABLE`.
  - Then `python -m pytest tests/ -q`.
  - This job must NOT set `continue-on-error` at the job level. A genuine break against the supported release is a real failure — which is exactly why the install must be variable-gated rather than hardcoded.
- Job `hermes-main`:
  - `runs-on: ubuntu-latest`, `continue-on-error: true` at the job level, so a break in unreleased Hermes reports without failing the workflow run.
  - Same steps, but the Hermes install step targets the development branch: `pip install "git+${{ vars.HERMES_GIT_URL }}@${HERMES_GIT_REF}"` with `HERMES_GIT_REF` defaulting to `main` (Hermes's actual default branch). Keep the skip-when-empty guard. `HERMES_GIT_URL` is provisioned to `https://github.com/NousResearch/hermes-agent` — a public MIT repository, so it is a repository variable, never a secret. Do not hardcode the URL in the workflow file; the variable indirection is what lets the URL change without a code edit.
- Job `report`:
  - `runs-on: ubuntu-latest`, `needs: [latest-release, hermes-main]`, `if: always()`.
  - A step that echoes both job results into `$GITHUB_STEP_SUMMARY` as a two-row markdown table, so the nightly outcome is readable without opening logs.
  - A second step that **fails** (exit 1) when either `vars.HERMES_DIST_NAME` or `vars.HERMES_GIT_URL` is empty, with a message naming the missing variable. Both are provisioned as of 2026-07-27, so this step should pass — but keep it. Without it, a future variable deletion would make the nightly report green while testing nothing, vacuously satisfying the Phase 1 criterion "CI runs against both latest release and current `main`" and the Phase 11 criterion "nightly compatibility CI against Hermes `main` is green".

Both workflows must contain no credential, token, or password literal. The only secret-like references permitted are `${{ vars.HERMES_GIT_URL }}` and `${{ vars.HERMES_DIST_NAME }}`, which are non-secret repository variables.

Validate both files parse as YAML before finishing. Note that `on:` is parsed by PyYAML as the boolean `True` unless quoted, so every gate reads it as `w.get('on', w.get(True))` — never `w[True]` alone, which raises `KeyError` when the key *is* quoted.

> verification: test -f .github/workflows/ci.yml && test -f .github/workflows/hermes-compat.yml
> verification: python -c "import yaml; [yaml.safe_load(open(f)) for f in ['.github/workflows/ci.yml','.github/workflows/hermes-compat.yml']]"
> verification: python -c "import yaml; w=yaml.safe_load(open('.github/workflows/ci.yml')); m=w['jobs']['test']['strategy']['matrix']['os']; assert set(m)=={'ubuntu-latest','macos-latest','windows-latest'}, m"
> verification: python -c "import yaml; w=yaml.safe_load(open('.github/workflows/ci.yml')); assert 'schema-validation' in w['jobs'], sorted(w['jobs']); assert w['permissions']['contents']=='read'; assert w['concurrency']['cancel-in-progress'] is True"
> verification: grep -riE '(^|[^a-z])(ruff|flake8|black|mypy)([^a-z]|$)' .github/workflows/ci.yml && exit 1 || exit 0
> verification: grep -q 'pip install hermes-agent' .github/workflows/hermes-compat.yml && exit 1 || exit 0
> verification: grep -q 'HERMES_DIST_NAME' .github/workflows/hermes-compat.yml
> verification: python -c "import yaml; w=yaml.safe_load(open('.github/workflows/hermes-compat.yml')); assert 'report' in w['jobs']; assert w['jobs']['report']['needs']==['latest-release','hermes-main']"
> verification: python -c "import yaml; w=yaml.safe_load(open('.github/workflows/hermes-compat.yml')); trig=w.get('on', w.get(True)); assert 'schedule' in trig and 'workflow_dispatch' in trig, trig"
> verification: python -c "import yaml; w=yaml.safe_load(open('.github/workflows/hermes-compat.yml')); assert w['jobs']['hermes-main'].get('continue-on-error') is True; assert w['jobs']['latest-release'].get('continue-on-error') is not True"
> verification: grep -qE '(ghp_|gho_|sk-[A-Za-z0-9]{10}|AKIA[0-9A-Z]{16})' .github/workflows/ci.yml .github/workflows/hermes-compat.yml && exit 1 || exit 0
> verification: grep -q 'windows-latest' .github/workflows/ci.yml
  </action>
  <verify>
test -f .github/workflows/ci.yml && test -f .github/workflows/hermes-compat.yml
python -c "import yaml; [yaml.safe_load(open(f)) for f in ['.github/workflows/ci.yml','.github/workflows/hermes-compat.yml']]"
python -c "import yaml; w=yaml.safe_load(open('.github/workflows/ci.yml')); m=w['jobs']['test']['strategy']['matrix']['os']; assert set(m)=={'ubuntu-latest','macos-latest','windows-latest'}, m"
python -c "import yaml; w=yaml.safe_load(open('.github/workflows/ci.yml')); assert 'schema-validation' in w['jobs'], sorted(w['jobs']); assert w['permissions']['contents']=='read'; assert w['concurrency']['cancel-in-progress'] is True"
grep -riE '(^|[^a-z])(ruff|flake8|black|mypy)([^a-z]|$)' .github/workflows/ci.yml && exit 1 || exit 0
grep -q 'pip install hermes-agent' .github/workflows/hermes-compat.yml && exit 1 || exit 0
grep -q 'HERMES_DIST_NAME' .github/workflows/hermes-compat.yml
python -c "import yaml; w=yaml.safe_load(open('.github/workflows/hermes-compat.yml')); assert 'report' in w['jobs']; assert w['jobs']['report']['needs']==['latest-release','hermes-main']"
python -c "import yaml; w=yaml.safe_load(open('.github/workflows/hermes-compat.yml')); trig=w.get('on', w.get(True)); assert 'schedule' in trig and 'workflow_dispatch' in trig, trig"
python -c "import yaml; w=yaml.safe_load(open('.github/workflows/hermes-compat.yml')); assert w['jobs']['hermes-main'].get('continue-on-error') is True; assert w['jobs']['latest-release'].get('continue-on-error') is not True"
grep -qE '(ghp_|gho_|sk-[A-Za-z0-9]{10}|AKIA[0-9A-Z]{16})' .github/workflows/ci.yml .github/workflows/hermes-compat.yml && exit 1 || exit 0
grep -q 'windows-latest' .github/workflows/ci.yml
  </verify>
  <done>The main CI workflow runs the test suite across Windows, macOS, and Linux on two Python versions with least-privilege permissions, and the nightly workflow probes compatibility against both the latest Hermes release and a configurable main ref, with the main job non-blocking and a summary job reporting both outcomes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `compatibility.py` imports cleanly with Hermes absent and imports nothing from `hermes` or other `hermes_auto` submodules
- [ ] `check_compatibility()` returns `COMPATIBLE` only after a successful parse and specifier match
- [ ] `VERSION_UNREADABLE`, `UNSUPPORTED_VERSION`, and `HERMES_NOT_INSTALLED` all report `is_usable` False
- [ ] Pre-release versions inside the range are treated as compatible
- [ ] Unit tests run without Hermes installed and contain no conditional skips
- [ ] `HERMES_DISTRIBUTION_NAME` is `hermes-agent` and `SUPPORTED_HERMES` is `>=0.19,<1.0`, both asserted by tests
- [ ] `detect_hermes_version()` returns a non-None value on this machine
- [ ] `ci.yml` matrix covers ubuntu-latest, macos-latest, and windows-latest
- [ ] `ci.yml` contains a `schema-validation` job, a `permissions` block, and a `concurrency` block, all asserted by a YAML gate
- [ ] No linter appears in `ci.yml`, and `expected_artifacts` no longer claims a lint job
- [ ] `hermes-compat.yml` runs on a schedule and on workflow_dispatch
- [ ] The Hermes install is gated on `vars.HERMES_DIST_NAME`, not hardcoded
- [ ] The `report` job fails when either repository variable is unprovisioned, so a vacuous green is impossible
- [ ] The `hermes-main` job is `continue-on-error: true`; the `latest-release` job is not
- [ ] No credential, token, or password literal appears in either workflow
- [ ] `pyproject.toml`, `tests/conftest.py`, and `tests/contract/` are unchanged
- [ ] `design.md` is unchanged (`git diff --exit-code design.md`)
- [ ] Every planned task is completed, blocked with evidence, or escalated; no planned work is self-deferred
</verification>

<success_criteria>
- `python -m pytest tests/unit/test_compatibility.py -q` exits 0 in an environment without Hermes installed
- Both workflow files parse with `yaml.safe_load`
- `PYTHONPATH=src python -c "from hermes_auto.compatibility import check_compatibility"` exits 0
- `git status --short` shows no changes to `pyproject.toml`, `tests/conftest.py`, `tests/contract/`, `docs/`, or `design.md`
</success_criteria>

<output>
After completion, create `.planning/phases/01-contracts-schemas-baselines/01-05-SUMMARY.md` recording: that `SUPPORTED_HERMES = ">=0.19,<1.0"` has an evidence-based lower bound (`NousResearch/hermes-agent` `pyproject.toml`, PyPI, and the local install all report `0.19.0`) while the upper bound remains a project decision because design.md states no bounds anywhere; the confirmed distribution name `hermes-agent` with the `importlib.metadata` output proving it; the CalVer-tags-versus-semver-distribution trap and the test that guards it; that `vars.HERMES_DIST_NAME` and `vars.HERMES_GIT_URL` were provisioned on 2026-07-27 so both nightly jobs genuinely measure; whether Hermes's `requires-python = ">=3.11,<3.14"` upper bound should be mirrored in this project's own `requires-python` when the optional Tier-4 credential bridge lands in Phase 7; and any workflow step deferred to Phase 11 packaging, including lint tooling.
</output>
