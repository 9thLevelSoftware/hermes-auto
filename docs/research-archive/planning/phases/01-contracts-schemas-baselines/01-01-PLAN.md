---
phase: 01-contracts-schemas-baselines
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - README.md
  - LICENSE
  - SECURITY.md
  - CHANGELOG.md
  - .gitignore
  - src/hermes_auto/__init__.py
  - src/hermes_auto/version.py
  - src/hermes_auto/py.typed
  - src/hermes_auto/gateway/__init__.py
  - src/hermes_auto/routing/__init__.py
  - src/hermes_auto/state/__init__.py
  - src/hermes_auto/inventory/__init__.py
  - src/hermes_auto/health/__init__.py
  - src/hermes_auto/adapters/__init__.py
  - src/hermes_auto/harnesses/__init__.py
  - src/hermes_auto/telemetry/__init__.py
  - src/hermes_auto/evaluation/__init__.py
  - src/hermes_auto/learning/__init__.py
  - src/hermes_auto/data/__init__.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/unit/__init__.py
  - tests/unit/test_package_layout.py
  - tests/property/__init__.py
  - tests/contract/__init__.py
  - tests/integration/__init__.py
  - tests/e2e/__init__.py
  - tests/fault/__init__.py
  - tests/performance/__init__.py
  - tests/fixtures/.gitkeep
files_forbidden:
  - design.md
  - .planning/
  - docs/
  - .github/
  - scripts/
  - src/hermes_auto/compatibility.py
  - src/hermes_auto/gateway/schemas.py
  - src/hermes_auto/data/schema/
sequential_files: []
expected_artifacts:
  - path: "pyproject.toml"
    provides: "Packaging metadata, complete dependency set for all of Phase 1, and pytest configuration"
    required: true
  - path: "src/hermes_auto/version.py"
    provides: "Single source of truth for the distribution version"
    required: true
  - path: "tests/unit/test_package_layout.py"
    provides: "Test proving every declared subpackage is importable"
    required: true
  - path: "SECURITY.md"
    provides: "Vulnerability disclosure policy and loopback-binding security posture statement"
    required: true
autonomous: false
agents: [engineering-senior-developer, testing-workflow-optimizer, project-manager-senior]
requirements: [R26]
user_setup:
  - "A dedicated project virtual environment MUST exist at `.venv/` before this plan runs. The default interpreter on this machine is the Hermes Agent venv (`sys.prefix` = `C:\\Users\\dasbl\\AppData\\Local\\hermes\\hermes-agent\\venv`), and installing into it would violate PROJECT.md's constraint that sidecar dependencies stay isolated from the Hermes environment. Task 3 creates `.venv` as its first step; if that fails, this plan is BLOCKED."
verification_commands:
  - "test -f pyproject.toml && test -f README.md && test -f LICENSE && test -f SECURITY.md && test -f CHANGELOG.md"
  - "test -x .venv/Scripts/python.exe || test -x .venv/bin/python"
  - "python -c \"import sys,pathlib; p=pathlib.Path('.venv').resolve(); import subprocess; exe=p/'Scripts'/'python.exe'; exe=exe if exe.exists() else p/'bin'/'python'; out=subprocess.run([str(exe),'-c','import sys;print(sys.prefix)'],capture_output=True,text=True).stdout.strip(); assert 'hermes-agent' not in out, out\""
  - "python -c \"import tomllib,sys; d=tomllib.load(open('pyproject.toml','rb')); n=d['project']['name']; sys.exit(0 if n=='hermes-auto-router' else 1)\""
  - "for p in gateway routing state inventory health adapters harnesses telemetry evaluation learning data; do test -f \"src/hermes_auto/$p/__init__.py\" || exit 1; done"
  - "for d in unit property contract integration e2e fault performance fixtures; do test -d \"tests/$d\" || exit 1; done"
  - "python -c \"import tomllib; d=tomllib.load(open('pyproject.toml','rb')); deps=' '.join(d['project']['dependencies']+d['project']['optional-dependencies']['dev']); assert all(k in deps for k in ('packaging','jsonschema','pyyaml','pytest','hypothesis')), deps\""
  - "for p in 'design.md' 'docs/' '.planning/'; do grep -q \"$p\" .gitignore && exit 1; done; exit 0"
  - "test $(wc -l < LICENSE) -ge 150"
  - "for s in '## Status' '## Install' '## Documentation'; do grep -q \"$s\" README.md || exit 1; done"
  - "for s in loopback bearer CORS retention; do grep -qi \"$s\" SECURITY.md || exit 1; done"

must_haves:
  truths:
    - "The distribution name is exactly 'hermes-auto-router' and the import package is exactly 'hermes_auto'."
    - "Every subpackage named in design.md §17 exists as an importable Python package with an __init__.py."
    - "pyproject.toml declares packaging, jsonschema, and pyyaml as runtime dependencies and pytest plus hypothesis as dev dependencies, so no later Phase 1 plan needs to edit it."
    - "The tests/ tree contains all eight directories from design.md §17 and pytest collects from tests/ without error."
    - "No module in src/hermes_auto/ contains routing, scoring, eligibility, or candidate-selection logic — only package declarations and version metadata."
  artifacts:
    - path: "pyproject.toml"
      provides: "Packaging metadata, full Phase 1 dependency set, pytest config"
      min_lines: 45
      contains: "hermes-auto-router"
    - path: "src/hermes_auto/version.py"
      provides: "Version constant"
      min_lines: 3
      contains: "__version__"
    - path: "tests/unit/test_package_layout.py"
      provides: "Importability test across all subpackages"
      min_lines: 20
      contains: "importlib"
  key_links:
    - from: "src/hermes_auto/__init__.py"
      to: "src/hermes_auto/version.py"
      via: "re-export of the version constant"
      pattern: "from .version import __version__"
    - from: "pyproject.toml"
      to: "src/hermes_auto/"
      via: "setuptools src-layout package discovery"
      pattern: "where.*src"
---

<objective>
Create the repository skeleton for the `hermes-auto-router` distribution: packaging metadata with the complete Phase 1 dependency set, the full `src/hermes_auto/` package tree from design.md §17, repository meta files, and the eight-directory `tests/` tree with working pytest configuration.

Purpose: every other plan in Phase 1 writes into this structure. It must exist and be importable before wire schemas (01-03), routing schemas (01-04), the compatibility probe (01-05), or the baseline harness (01-07) can be built.
Output: `pyproject.toml`, `src/hermes_auto/` package tree, `tests/` tree, and repository meta files.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-contracts-schemas-baselines/01-CONTEXT.md

Relevant source files:
@design.md — read §17 (Repository structure) for the exact directory tree, §18 Phase 0 for scope boundaries, and §23 for what must NOT appear in this release. Read-only.
</context>

<execution_contract>
Harness: read-before-write -> evidence-before-action -> minimal diff -> verify-before-report

Role: `engineering-senior-developer` is the primary executor and owns `pyproject.toml` and the `src/` tree. `testing-workflow-optimizer` owns the `tests/` tree and pytest configuration. `project-manager-senior` is the mandatory cross-division coordinator (the phase spans Engineering, Testing, Product, and Specialized divisions) and is responsible for confirming that the created structure covers every path later plans in this phase declare in their `files_modified`.
Task: Deliver the repository skeleton satisfying R26's packaging and structure surface. This plan introduces no routing behavior.
Scope:
- Read targets: `design.md` §17 and §18 Phase 0; `.planning/phases/01-contracts-schemas-baselines/01-CONTEXT.md`; `.planning/PROJECT.md` § Constraints.
- Write targets: exactly the paths in `files_modified`.
- Forbidden targets: `design.md`, `.planning/`, `docs/`, `.github/`, `scripts/`, `src/hermes_auto/compatibility.py`, `src/hermes_auto/gateway/schemas.py`, `src/hermes_auto/data/schema/`. The last four belong to plans 01-05, 01-03, and 01-03/01-04 respectively.
Allowed tools/actions:
- Read `design.md` and the planning files.
- Create the files listed in `files_modified`.
- Run `python -c`, `python -m pytest`, and `python -m pip install -e .` to verify.
Forbidden actions:
- Do not modify files outside `files_modified`.
- Do not create `docs/` — plan 01-02 owns that directory in this same wave.
- Do not add dependencies beyond the set named in Task 1; do not add web frameworks, HTTP clients, ML libraries, or LiteLLM in this phase.
- Do not write routing, scoring, eligibility, cost, or candidate-selection logic. Subpackage `__init__.py` files carry only a docstring and, where noted, `__all__`.
- Do not self-defer planned work.
Implementation sequence:
1. Read `design.md` §17 to confirm the exact subpackage names, then read §23 "Exclude initially" to confirm what must not be scaffolded.
2. Create `pyproject.toml` with the full dependency set (Task 1).
3. Create the `src/hermes_auto/` package tree and meta files (Task 2).
4. Create `.venv`, then the `tests/` tree, `conftest.py`, and the layout test (Task 3).

**Interpreter rule — applies to every command in this plan and every later Phase 1 plan.** The machine default interpreter is the Hermes Agent venv. After Task 3 creates `.venv`, all `python`, `pip`, and `pytest` invocations use `.venv/Scripts/python.exe` (Windows) or `.venv/bin/python` (POSIX). Never install into the default interpreter. All verification commands are POSIX shell — run them through the Bash tool (GNU bash 5.3.15 is available on this host), never PowerShell, because `test`, `for … done`, and inline `VAR=x cmd` prefixes do not exist in PowerShell.

**Scope of the §17 tree in this plan.** This plan creates the **eleven subpackages only**. design.md §17 also lists seven top-level modules — `plugin.py`, `provider.py`, `commands.py`, `cli.py`, `config.py`, `supervisor.py`, `compatibility.py`. Do not create any of them: the first six arrive in Phases 2 through 7, and `compatibility.py` belongs to plan 01-05 in this same phase. Their absence is correct and must not trigger a stop gate.
Required interfaces/content structure:
- `pyproject.toml`: `[build-system]` using setuptools>=68 with wheel; `[project]` with `name = "hermes-auto-router"`, `version` read dynamically from `src/hermes_auto/version.py`, `requires-python = ">=3.11"`, `dependencies = ["packaging>=23.0", "jsonschema>=4.20", "pyyaml>=6.0"]`, `[project.optional-dependencies] dev = ["pytest>=8.0", "pytest-asyncio>=0.23", "hypothesis>=6.98"]`; `[tool.setuptools.packages.find] where = ["src"]`; `[tool.pytest.ini_options]` with `testpaths = ["tests"]` and `pythonpath = ["src"]`.
- `src/hermes_auto/version.py`: exactly one public name, `__version__: str = "0.1.0"`.
- `src/hermes_auto/__init__.py`: docstring plus `from .version import __version__` and `__all__ = ["__version__"]`.
- Each subpackage `__init__.py`: a one-line docstring naming the module's Phase-N responsibility per design.md §5.4. No imports of unwritten modules.
Edge/error cases:
- If `python -m pip install -e .` is unavailable in the environment, fall back to verifying importability with `PYTHONPATH=src python -c "import hermes_auto"` and record which method was used.
- `.gitignore` must not exclude `design.md`, `docs/`, or `.planning/` — those are tracked project content.
- If a directory in `files_modified` already exists with content, do not delete it; report `BLOCKED`.
Verification criteria:
- All six commands in `verification_commands` exit 0.
- `python -m pytest tests/unit/test_package_layout.py` passes.
Final result format:
- Status: Complete | Partial | Failed | BLOCKED
- Files changed
- Verification commands and outputs
- Decisions made
- Issues/errors
</execution_contract>

<stop_gates>
Emit `BLOCKED` and stop instead of guessing when:
- `design.md` is missing or unreadable, or its §17 **eleven subpackage names** do not match the subpackage list in `files_modified`. The seven top-level modules in §17 are deliberately out of scope — their absence is never a blocker.
- `.venv` cannot be created, or the resolved interpreter's `sys.prefix` contains `hermes-agent` — installing into the Hermes Agent environment violates PROJECT.md's isolation constraint and must halt the plan.
- Completing a task requires creating a file not listed in `files_modified` — in particular, if you conclude a dependency must be added that is not in the Task 1 list.
- Any instruction conflicts with `files_forbidden` (for example, if you conclude `docs/` must be created here).
- The Python version available cannot satisfy `requires-python = ">=3.11"`.
- `tomllib` is unavailable, making the `pyproject.toml` verification command unrunnable, and no equivalent deterministic check can be substituted.
- Verification commands fail after one focused fix attempt.
</stop_gates>

<recovery>
After compaction, interruption, or context loss:
1. Re-read this PLAN.md, `.planning/phases/01-contracts-schemas-baselines/01-CONTEXT.md`, and any `01-01-SUMMARY.md` already written.
2. Run `git status --short` and `git diff --stat`; inspect `git diff` for every file already changed.
3. Compare changed files against `files_modified` and `files_forbidden`; if any unapproved change exists, emit `BLOCKED`.
4. Re-run the verification commands for tasks already marked done.
5. Continue only from the next unverified task; do not rely on memory-only claims.
</recovery>

<tasks>

<task type="auto">
  <name>Task 1: Create pyproject.toml with the complete Phase 1 dependency set</name>
  <files>pyproject.toml, .gitignore</files>
  <action>
Read `design.md` §17 to confirm the distribution is named `hermes-auto-router` with a `src/hermes_auto/` layout, and §23 "Exclude initially" to confirm no ML or LiteLLM dependencies belong in this phase.

Create `pyproject.toml` at the repository root with exactly these sections:

- `[build-system]`: `requires = ["setuptools>=68", "wheel"]`, `build-backend = "setuptools.build_meta"`.
- `[project]`: `name = "hermes-auto-router"`, `description = "Capability-, cost-, cache-, and outcome-aware model routing for Hermes Agent"`, `readme = "README.md"`, `requires-python = ">=3.11"`, `license = {file = "LICENSE"}`, `dynamic = ["version"]`.
- `[project].dependencies`: `packaging>=23.0` (used by `compatibility.py` in plan 01-05 for version-range parsing), `jsonschema>=4.20` (used by the schema validation tests in plans 01-03 and 01-04), `pyyaml>=6.0` (used by the baseline corpus loader in plan 01-07).
- `[project.optional-dependencies]`: `dev = ["pytest>=8.0", "pytest-asyncio>=0.23", "hypothesis>=6.98"]`.
- `[tool.setuptools.dynamic]`: `version = {attr = "hermes_auto.version.__version__"}`.
- `[tool.setuptools.packages.find]`: `where = ["src"]`.
- `[tool.setuptools.package-data]`: `hermes_auto = ["py.typed", "data/schema/**/*.json", "data/*.yaml"]` so schema files from plans 01-03 and 01-04 ship with the wheel.
- `[tool.pytest.ini_options]`: `testpaths = ["tests"]`, `pythonpath = ["src"]`, `markers = ["contract: protocol contract tests", "performance: latency and throughput tests"]`.

This dependency set is deliberately complete for all of Phase 1. Plans 01-03, 01-04, 01-05, and 01-07 declare `pyproject.toml` as forbidden and rely on these declarations. Do not add anything beyond this list — no HTTP framework, no HTTP client, no ONNX runtime, no LiteLLM. Those arrive in Phase 2 and later.

Create `.gitignore` covering `__pycache__/`, `*.py[cod]`, `.pytest_cache/`, `build/`, `dist/`, `*.egg-info/`, `.venv/`, `venv/`, `.coverage`, `htmlcov/`, and `*.sqlite3`. It must NOT ignore `design.md`, `docs/`, or `.planning/` — those are tracked project content and the verification loop checks all three, not just `design.md`.

Do NOT add a linter (`ruff`, `flake8`, `black`, `mypy`) to the dev extra. Lint tooling arrives in Phase 11 per R25. Plan 01-05 has been corrected so its CI workflow no longer claims a lint job.

If you determine an additional dependency is genuinely required to satisfy this phase, emit `BLOCKED` naming the dependency and the plan that needs it — do not add it unilaterally.

> verification: test -f pyproject.toml
> verification: python -c "import tomllib,sys; d=tomllib.load(open('pyproject.toml','rb')); sys.exit(0 if d['project']['name']=='hermes-auto-router' else 1)"
> verification: python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); deps=' '.join(d['project']['dependencies']+d['project']['optional-dependencies']['dev']); assert all(k in deps for k in ('packaging','jsonschema','pyyaml','pytest','hypothesis')), deps"
> verification: python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); deps=' '.join(d['project']['optional-dependencies']['dev']); assert not any(k in deps for k in ('ruff','flake8','black','mypy')), deps"
> verification: python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert d['tool']['setuptools']['packages']['find']['where']==['src']"
> verification: test -f .gitignore
> verification: for p in 'design.md' 'docs/' '.planning/'; do grep -q "$p" .gitignore && exit 1; done; exit 0
  </action>
  <verify>
test -f pyproject.toml
python -c "import tomllib,sys; d=tomllib.load(open('pyproject.toml','rb')); sys.exit(0 if d['project']['name']=='hermes-auto-router' else 1)"
python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); deps=' '.join(d['project']['dependencies']+d['project']['optional-dependencies']['dev']); assert all(k in deps for k in ('packaging','jsonschema','pyyaml','pytest','hypothesis')), deps"
python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); deps=' '.join(d['project']['optional-dependencies']['dev']); assert not any(k in deps for k in ('ruff','flake8','black','mypy')), deps"
python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert d['tool']['setuptools']['packages']['find']['where']==['src']"
test -f .gitignore
for p in 'design.md' 'docs/' '.planning/'; do grep -q "$p" .gitignore && exit 1; done; exit 0
  </verify>
  <done>pyproject.toml declares the hermes-auto-router distribution with src-layout discovery, the complete Phase 1 dependency set, and pytest configuration, and .gitignore excludes build artifacts without excluding tracked project content.</done>
</task>

<task type="auto">
  <name>Task 2: Create the src/hermes_auto package tree and repository meta files</name>
  <files>src/hermes_auto/__init__.py, src/hermes_auto/version.py, src/hermes_auto/py.typed, src/hermes_auto/{gateway,routing,state,inventory,health,adapters,harnesses,telemetry,evaluation,learning,data}/__init__.py, README.md, LICENSE, SECURITY.md, CHANGELOG.md</files>
  <action>
Read `design.md` §17 for the subpackage list and §5.4 for what each gateway module group is responsible for.

Create `src/hermes_auto/version.py` containing a module docstring and exactly one public name:
```python
__version__: str = "0.1.0"
```

Create `src/hermes_auto/__init__.py` with a module docstring describing the distribution, then:
```python
from .version import __version__

__all__ = ["__version__"]
```

Create an empty `src/hermes_auto/py.typed` marker file.

Create `__init__.py` in each of these eleven subpackages: `gateway`, `routing`, `state`, `inventory`, `health`, `adapters`, `harnesses`, `telemetry`, `evaluation`, `learning`, `data`. Each file contains ONLY a one-line docstring naming that subpackage's responsibility and the phase that implements it. For example:

```python
"""Routing pipeline: features, requirements, eligibility, scoring, decisions (Phase 4)."""
```

Do not import submodules that do not exist yet — a bare docstring is the entire file. Do not add `__all__` to subpackages. Do not write any routing, scoring, cost, eligibility, or candidate-selection code; design.md §18 Phase 0 exit criteria require that no production routing has been introduced.

Create these repository meta files:

- `README.md`: title, a two-paragraph description drawn from PROJECT.md § What This Is, an architecture diagram reproduced from design.md §1 (the ASCII block), a "Status" section stating the project is in Phase 1 (contracts and schemas, pre-implementation), an "Install" section marked as not yet available, and a "Documentation" section linking to `docs/architecture.md`, `docs/threat-model.md`, and `docs/privacy.md` (created by plans 01-02 and 01-06 — the links are forward references and that is intended).
- `LICENSE`: the full Apache License 2.0 text, copyright 2026 9th Level Software.
- `SECURITY.md`: a supported-versions table, a vulnerability disclosure process with a reporting address placeholder marked `TODO: security contact`, and a "Security posture" section stating the four hard invariants from PROJECT.md § Constraints: loopback-only binding, generated bearer token with restrictive file permissions, no CORS, and no raw prompt or secret retention by default.
- `CHANGELOG.md`: Keep a Changelog format with an `## [Unreleased]` section containing an `### Added` subsection listing "Repository skeleton and packaging contract".

> verification: test -f src/hermes_auto/version.py && test -f src/hermes_auto/py.typed
> verification: grep -q '__version__' src/hermes_auto/version.py
> verification: grep -q 'from .version import __version__' src/hermes_auto/__init__.py
> verification: for p in gateway routing state inventory health adapters harnesses telemetry evaluation learning data; do test -f "src/hermes_auto/$p/__init__.py" || exit 1; done
> verification: test -f README.md && test -f LICENSE && test -f SECURITY.md && test -f CHANGELOG.md
> verification: grep -qi 'apache' LICENSE
> verification: test $(wc -l < LICENSE) -ge 150
> verification: for s in '## Status' '## Install' '## Documentation'; do grep -q "$s" README.md || exit 1; done
> verification: for s in loopback bearer CORS retention; do grep -qi "$s" SECURITY.md || exit 1; done
> verification: test ! -e src/hermes_auto/plugin.py && test ! -e src/hermes_auto/cli.py && test ! -e src/hermes_auto/compatibility.py
> verification: PYTHONPATH=src python -c "import hermes_auto; assert hermes_auto.__version__"
  </action>
  <verify>
test -f src/hermes_auto/version.py && test -f src/hermes_auto/py.typed
grep -q '__version__' src/hermes_auto/version.py
grep -q 'from .version import __version__' src/hermes_auto/__init__.py
for p in gateway routing state inventory health adapters harnesses telemetry evaluation learning data; do test -f "src/hermes_auto/$p/__init__.py" || exit 1; done
test -f README.md && test -f LICENSE && test -f SECURITY.md && test -f CHANGELOG.md
grep -qi 'apache' LICENSE
test $(wc -l < LICENSE) -ge 150
for s in '## Status' '## Install' '## Documentation'; do grep -q "$s" README.md || exit 1; done
for s in loopback bearer CORS retention; do grep -qi "$s" SECURITY.md || exit 1; done
test ! -e src/hermes_auto/plugin.py && test ! -e src/hermes_auto/cli.py && test ! -e src/hermes_auto/compatibility.py
PYTHONPATH=src python -c "import hermes_auto; assert hermes_auto.__version__"
  </verify>
  <done>The hermes_auto package imports cleanly, exposes __version__, declares all eleven subpackages from design.md §17 as importable packages containing no routing logic, and the four repository meta files exist with the required sections.</done>
</task>

<task type="auto">
  <name>Task 3: Create the tests tree, conftest, and package layout test</name>
  <files>tests/__init__.py, tests/conftest.py, tests/unit/test_package_layout.py, and the eight tests/ subdirectories</files>
  <action>
Read `design.md` §17 for the `tests/` tree and §20 for what each test category will eventually cover.

Create these eight directories under `tests/`: `unit`, `property`, `contract`, `integration`, `e2e`, `fault`, `performance`, `fixtures`. Each directory except `fixtures` gets an `__init__.py` containing a one-line docstring naming what design.md §20 assigns to it (for example, `"""Property tests for routing invariants (design.md §20.2)."""`). `fixtures/` gets a `.gitkeep` file instead of `__init__.py`, because plans 01-03, 01-04, and 01-07 will add data files there rather than test modules.

Create `tests/__init__.py` with a one-line docstring.

Create `tests/conftest.py` defining exactly two module-scoped fixtures and no test logic:
- `repo_root() -> pathlib.Path` — returns the repository root, resolved as `pathlib.Path(__file__).resolve().parent.parent`.
- `schema_dir(repo_root) -> pathlib.Path` — returns `repo_root / "src" / "hermes_auto" / "data" / "schema"`. This fixture is consumed by plans 01-03 and 01-04; it must not assert the directory exists, because it does not exist yet in this wave.

Create `tests/unit/test_package_layout.py` with three test functions:
- `test_top_level_package_imports()` — imports `hermes_auto` and asserts `__version__` is a non-empty string.
- `test_all_subpackages_importable()` — iterates the eleven subpackage names from a module-level `EXPECTED_SUBPACKAGES` tuple, imports each with `importlib.import_module(f"hermes_auto.{name}")`, and asserts no ImportError. The tuple must contain exactly: `gateway`, `routing`, `state`, `inventory`, `health`, `adapters`, `harnesses`, `telemetry`, `evaluation`, `learning`, `data`.
- `test_test_tree_directories_exist(repo_root)` — asserts all eight `tests/` subdirectories exist, using the `repo_root` fixture.

Do not write tests for routing behavior, schema content, or CLI commands — none of that exists yet, and asserting on it would produce failing tests that block the wave.

**Environment isolation — do this FIRST, before any install.** The machine default interpreter is the Hermes Agent venv (`sys.prefix` = `C:\Users\dasbl\AppData\Local\hermes\hermes-agent\venv`) and `pytest` is not installed in it. Installing this project there would violate PROJECT.md's constraint that sidecar dependencies stay isolated from the Hermes environment.

1. Run `python -m venv .venv`.
2. Resolve the venv interpreter: `.venv/Scripts/python.exe` on Windows, `.venv/bin/python` on POSIX. Call it `$VPY` for the rest of this task.
3. Assert isolation: `$VPY -c "import sys; assert 'hermes-agent' not in sys.prefix, sys.prefix"`. If this fails, emit `BLOCKED` — do not proceed with any install.
4. Install into the venv only: `$VPY -m pip install --upgrade pip` then `$VPY -m pip install -e ".[dev]"`.
5. Run the suite with the venv interpreter: `$VPY -m pytest tests/ -q`.

If `python -m venv` is unavailable, emit `BLOCKED` with the error. Do not fall back to installing into the default interpreter, and do not fall back to a bare `PYTHONPATH=src` run as a substitute for the install — the install is what proves the packaging contract works.

**Wheel-content check.** After the suite passes, build a wheel and confirm the package data declared in Task 1 actually ships. Run `$VPY -m pip wheel --no-deps -w dist-check .` then assert the wheel contains `hermes_auto/py.typed`. Plans 01-03 and 01-04 add JSON schema files under `hermes_auto/data/schema/`; record in the summary that this check must be re-run after those plans land, because `package-data` globs are a common silent packaging failure — `load_schemas()` would return `{}` from an installed copy while every in-repo `PYTHONPATH=src` gate stayed green. Delete `dist-check/` afterward and confirm it is covered by `.gitignore`'s `dist/` entry or add `dist-check/` to `.gitignore`.

> verification: test -x .venv/Scripts/python.exe || test -x .venv/bin/python
> verification: for d in unit property contract integration e2e fault performance fixtures; do test -d "tests/$d" || exit 1; done
> verification: test -f tests/conftest.py && test -f tests/unit/test_package_layout.py
> verification: test -f tests/fixtures/.gitkeep
> verification: grep -q 'def repo_root' tests/conftest.py && grep -q 'def schema_dir' tests/conftest.py
> verification: grep -q 'importlib' tests/unit/test_package_layout.py
> verification: python -c "import pathlib,subprocess,sys; p=pathlib.Path('.venv').resolve(); e=p/'Scripts'/'python.exe'; e=e if e.exists() else p/'bin'/'python'; o=subprocess.run([str(e),'-c','import sys;print(sys.prefix)'],capture_output=True,text=True).stdout.strip(); assert 'hermes-agent' not in o, o"
> verification: python -c "import pathlib,subprocess,sys; p=pathlib.Path('.venv').resolve(); e=p/'Scripts'/'python.exe'; e=e if e.exists() else p/'bin'/'python'; sys.exit(subprocess.run([str(e),'-m','pytest','tests/','-q']).returncode)"
  </action>
  <verify>
test -x .venv/Scripts/python.exe || test -x .venv/bin/python
for d in unit property contract integration e2e fault performance fixtures; do test -d "tests/$d" || exit 1; done
test -f tests/conftest.py && test -f tests/unit/test_package_layout.py
test -f tests/fixtures/.gitkeep
grep -q 'def repo_root' tests/conftest.py && grep -q 'def schema_dir' tests/conftest.py
grep -q 'importlib' tests/unit/test_package_layout.py
python -c "import pathlib,subprocess,sys; p=pathlib.Path('.venv').resolve(); e=p/'Scripts'/'python.exe'; e=e if e.exists() else p/'bin'/'python'; o=subprocess.run([str(e),'-c','import sys;print(sys.prefix)'],capture_output=True,text=True).stdout.strip(); assert 'hermes-agent' not in o, o"
python -c "import pathlib,subprocess,sys; p=pathlib.Path('.venv').resolve(); e=p/'Scripts'/'python.exe'; e=e if e.exists() else p/'bin'/'python'; sys.exit(subprocess.run([str(e),'-m','pytest','tests/','-q']).returncode)"
  </verify>
  <done>An isolated .venv exists and provably is not the Hermes Agent environment, the eight-directory tests tree from design.md §17 exists, conftest.py provides the repo_root and schema_dir fixtures that later Phase 1 plans consume, pytest passes under the venv interpreter, and a built wheel is confirmed to carry the declared package data.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pyproject.toml` parses and declares name `hermes-auto-router` with src-layout discovery
- [ ] The dependency set includes packaging, jsonschema, pyyaml, pytest, and hypothesis so no later Phase 1 plan needs to edit `pyproject.toml`
- [ ] `hermes_auto` imports and exposes a non-empty `__version__`
- [ ] All eleven subpackages from design.md §17 are importable
- [ ] All eight `tests/` subdirectories exist and pytest collects from them
- [ ] `tests/conftest.py` exposes `repo_root` and `schema_dir` fixtures
- [ ] README.md, LICENSE, SECURITY.md, and CHANGELOG.md exist with their required sections
- [ ] `.venv` exists and its `sys.prefix` provably does not contain `hermes-agent`
- [ ] The editable install and the test suite both ran under the venv interpreter, not the machine default
- [ ] A built wheel carries `hermes_auto/py.typed`, and the summary records that the check must re-run after 01-03/01-04 add schema files
- [ ] No linter was added to the dev extra
- [ ] None of design.md §17's seven top-level modules (`plugin.py`, `provider.py`, `commands.py`, `cli.py`, `config.py`, `supervisor.py`, `compatibility.py`) were created
- [ ] No file under `src/hermes_auto/` contains routing, scoring, eligibility, or candidate-selection logic
- [ ] `docs/`, `.github/`, `scripts/`, and `src/hermes_auto/data/schema/` were not created by this plan
- [ ] `design.md` is unchanged (`git diff --exit-code design.md`)
- [ ] Every planned task is completed, blocked with evidence, or escalated; no planned work is self-deferred
</verification>

<success_criteria>
- `.venv/Scripts/python.exe -m pip install -e ".[dev]"` succeeds inside the isolated venv
- `.venv/Scripts/python.exe -m pytest tests/ -q` exits 0
- The venv interpreter's `sys.prefix` does not contain `hermes-agent`
- `python -c "import tomllib; tomllib.load(open('pyproject.toml','rb'))"` exits 0
- Every path that plans 01-03, 01-04, 01-05, and 01-07 declare in their `files_modified` has an existing parent package or directory, except the `data/schema/` and `.github/` trees those plans create themselves
- `git status --short` shows no modifications to `design.md` or `.planning/`
</success_criteria>

<output>
After completion, create `.planning/phases/01-contracts-schemas-baselines/01-01-SUMMARY.md` recording: the resolved Python version, the absolute path of the created `.venv` and its `sys.prefix`, the exact venv interpreter path that later plans must use, the final dependency list, the wheel-content check result together with the note that it must be re-run after plans 01-03 and 01-04 add schema files, and any subpackage name that differed from design.md §17.
</output>
