{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hermes-auto-router.dev/schema/wire/openai-chat-request.v1.json",
  "title": "OpenAI Chat Completions Request (hermes-auto-router wire subset, v1)",
  "description": "The subset of the OpenAI Chat Completions request body that the local routing gateway accepts at POST /v1/chat/completions (design.md 5.3). The gateway must stay OpenAI Chat Completions compatible because Hermes's normal chat transport already sends OpenAI-formatted messages and tools. Unknown top-level keys are permitted (additionalProperties: true) because providers add fields and the gateway must forward requests it can still relay rather than rejecting them; every key this schema does define is strictly typed. Fields deliberately left untyped-but-accepted include n, presence_penalty, frequency_penalty, logit_bias, logprobs, top_logprobs, parallel_tool_calls, service_tier, store, metadata, reasoning_effort, modalities, audio, prediction, and the deprecated functions/function_call pair.",
  "type": "object",
  "required": ["model", "messages"],
  "additionalProperties": true,
  "properties": {
    "model": {
      "type": "string",
      "minLength": 1,
      "description": "Requested model. For this gateway this is normally a virtual alias such as auto:balanced (design.md 5.1)."
    },
    "messages": {
      "type": "array",
      "minItems": 1,
      "description": "Conversation so far, in OpenAI message order.",
      "items": { "$ref": "#/$defs/message" }
    },
    "tools": {
      "type": "array",
      "description": "Tool definitions available to the model for this turn.",
      "items": { "$ref": "#/$defs/toolDefinition" }
    },
    "tool_choice": { "$ref": "#/$defs/toolChoice" },
    "stream": {
      "type": "boolean",
      "description": "Whether the response is delivered as an SSE stream. Not required; both true and false are valid. When true the response is a sequence of chunks matching sse-stream-contract.v1."
    },
    "stream_options": {
      "type": "object",
      "description": "Streaming modifiers. include_usage requests a final chunk carrying token usage (design.md 20.3, 'usage in final stream chunks').",
      "additionalProperties": true,
      "properties": {
        "include_usage": { "type": "boolean" }
      }
    },
    "max_tokens": {
      "type": "integer",
      "minimum": 1,
      "description": "Legacy output-token cap."
    },
    "max_completion_tokens": {
      "type": "integer",
      "minimum": 1,
      "description": "Current output-token cap; supersedes max_tokens on newer models."
    },
    "temperature": { "type": "number", "minimum": 0, "maximum": 2 },
    "top_p": { "type": "number", "minimum": 0, "maximum": 1 },
    "stop": {
      "description": "One stop sequence or a list of them.",
      "oneOf": [
        { "type": "string" },
        { "type": "array", "items": { "type": "string" } }
      ]
    },
    "response_format": { "$ref": "#/$defs/responseFormat" },
    "seed": { "type": "integer" },
    "user": { "type": "string" },
    "_hermes_auto": {
      "type": "object",
      "description": "Routing metadata injected by the Hermes provider plugin via build_extra_body(). Intentionally opaque here: its full shape is defined by routing/hermes-auto-metadata.v1.schema.json, and duplicating those properties would create two sources of truth. Permitted but never required. Per design.md 5.1 the gateway strips this key before forwarding the request upstream."
    }
  },
  "$defs": {
    "message": {
      "type": "object",
      "description": "One chat message. Tool results use role=tool and must carry the tool_call_id they answer; only assistant messages may carry tool_calls.",
      "required": ["role"],
      "additionalProperties": true,
      "properties": {
        "role": {
          "enum": ["system", "developer", "user", "assistant", "tool"]
        },
        "content": { "$ref": "#/$defs/messageContent" },
        "name": { "type": "string" },
        "tool_calls": {
          "type": "array",
          "description": "Tool calls emitted by the assistant. More than one entry represents parallel tool calls (design.md 20.3).",
          "items": { "$ref": "#/$defs/toolCall" }
        },
        "tool_call_id": {
          "type": "string",
          "description": "Identifier of the assistant tool call this message answers. Required on role=tool messages."
        },
        "refusal": { "type": ["string", "null"] }
      },
      "allOf": [
        {
          "if": {
            "properties": { "role": { "const": "tool" } },
            "required": ["role"]
          },
          "then": { "required": ["tool_call_id"] }
        },
        {
          "if": {
            "properties": { "role": { "const": "assistant" } },
            "required": ["role"]
          },
          "else": { "not": { "required": ["tool_calls"] } }
        }
      ]
    },
    "messageContent": {
      "description": "Message body: a plain string, null (assistant messages that only emit tool calls), or an ordered array of multipart content parts.",
      "anyOf": [
        { "type": "string" },
        { "type": "null" },
        {
          "type": "array",
          "items": { "$ref": "#/$defs/contentPart" }
        }
      ]
    },
    "contentPart": {
      "description": "One part of a multipart message body. Image parts are what make the vision cases in design.md 20.3 expressible.",
      "oneOf": [
        {
          "type": "object",
          "required": ["type", "text"],
          "additionalProperties": false,
          "properties": {
            "type": { "const": "text" },
            "text": { "type": "string" }
          }
        },
        {
          "type": "object",
          "required": ["type", "image_url"],
          "additionalProperties": false,
          "properties": {
            "type": { "const": "image_url" },
            "image_url": {
              "type": "object",
              "required": ["url"],
              "additionalProperties": false,
              "properties": {
                "url": {
                  "type": "string",
                  "minLength": 1,
                  "description": "An https URL or a data: URI carrying the image bytes."
                },
                "detail": { "enum": ["auto", "low", "high"] }
              }
            }
          }
        }
      ]
    },
    "toolCall": {
      "type": "object",
      "description": "One assistant tool call. Note that function.arguments is a JSON-encoded string, not an object; that is precisely why streamed arguments arrive fragmented and must be concatenated before parsing (design.md 20.3).",
      "required": ["id", "type", "function"],
      "additionalProperties": true,
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "type": { "const": "function" },
        "function": {
          "type": "object",
          "required": ["name", "arguments"],
          "additionalProperties": true,
          "properties": {
            "name": { "type": "string", "minLength": 1 },
            "arguments": {
              "type": "string",
              "description": "JSON-encoded argument object, carried as a string."
            }
          }
        }
      }
    },
    "toolDefinition": {
      "type": "object",
      "required": ["type", "function"],
      "additionalProperties": true,
      "properties": {
        "type": { "const": "function" },
        "function": {
          "type": "object",
          "required": ["name"],
          "additionalProperties": true,
          "properties": {
            "name": { "type": "string", "minLength": 1 },
            "description": { "type": "string" },
            "parameters": {
              "type": "object",
              "description": "JSON Schema describing the tool's arguments."
            },
            "strict": { "type": ["boolean", "null"] }
          }
        }
      }
    },
    "toolChoice": {
      "description": "How the model should use tools: a mode keyword, or an object naming one function.",
      "oneOf": [
        { "enum": ["none", "auto", "required"] },
        {
          "type": "object",
          "required": ["type", "function"],
          "additionalProperties": true,
          "properties": {
            "type": { "const": "function" },
            "function": {
              "type": "object",
              "required": ["name"],
              "additionalProperties": true,
              "properties": {
                "name": { "type": "string", "minLength": 1 }
              }
            }
          }
        }
      ]
    },
    "responseFormat": {
      "type": "object",
      "description": "Structured-output request (design.md 20.3, 'structured outputs').",
      "required": ["type"],
      "additionalProperties": true,
      "properties": {
        "type": { "enum": ["text", "json_object", "json_schema"] },
        "json_schema": { "type": "object" }
      }
    }
  }
}
